{% extends model('component') %}

{% define config = {
    name: 'antelope-box',
    tag: 'div'
} %}

{% define data = {
    name: required
} %}

{% block body %}
    {% if data.name %}
    <h1 class="{{config.name}}__name">--- {{ data.name }} ---</h1>
    {% endif %}
{% endblock %}

